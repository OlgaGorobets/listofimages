import React from 'react';
import styled from 'styled-components';

const About = () => {
  return <Heading>page about</Heading>;
};

export default About;

const Heading = styled.h1`
  font-size: 2rem;
  font-weight: 800;
  text-decoration: underline;
`;